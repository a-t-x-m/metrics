{
  "name": "@atxmtx/metrics",
  "version": "0.0.0",
  "description": "Sends events to Google Analytics from within Atom",
  "license": "MIT OR GPL-2.0",
  "scripts": {
    "build": "tsc --pretty --project ./",
    "dev": "tsc --watch --pretty --project ./",
    "lint": "tslint --project ./",
    "lint:fix": "tslint --fix --project ./",
    "test": "ava ./test/index.js --verbose"
  },
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/atxmtx/metrics.git"
  },
  "keywords": [
    "atom",
    "atom package"
  ],
  "dependencies": {
    "@atxmtx/developer-console": "^0.1.1",
    "address": "^1.1.2",
    "query-string": "^6.11.0",
    "uuid": "^3.4.0"
  },
  "devDependencies": {
    "@types/atom": "^1.36.2",
    "@types/node": "^13.7.4",
    "husky": "^4.2.0",
    "tslint": "^6.0.0",
    "typescript": "^3.8.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  }
}
